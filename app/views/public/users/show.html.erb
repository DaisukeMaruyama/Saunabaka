<div class="container">
  <div class="row">
    <div class="col-12 mt-4 text-center">
      <h2 class="user-heading"><%= @user.name %>さんのページ</h2>
      <p>登録情報の確認・更新ができます。</p>
    </div>
    <div class="col-md-6">
      <p class="user-item">名前（ニックネームでも可）</p>
      <%= @user.name %>
      <p class="user-item">メールアドレス</p>  
      <%= @user.email %><br>
      <%= link_to "ユーザー情報編集", edit_user_path, class:"btn btn-success mt-4" %><br>
      <%= link_to "ログインパスワード変更", edit_user_path, class:"btn btn-info mt-3" %>
      <%# とりあえず、edit_user_path　後でパスワード変更用の画面を作成 %>
    </div>
    <div class="col-md-6">
      <h4 class="user-favorite-heading mt-3 mb-3">お気に入りサウナ</h4>
      <% @favorite_list.each do |favorite| %>
        <p class="user-favorite-item">・<%= favorite.sauna_name %>　室温：<%= favorite.temperature %>℃　水風呂：<%= favorite.temperature %>℃　<%= favorite.prefecture %><br>
          <%= link_to "詳細", facility_path(favorite.id), class:"btn" %>
        </p>
      <% end %>
    </div>
  </div>
</div>
