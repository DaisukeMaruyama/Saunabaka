<div class="container">
  <div class="row">
    <div class="col-12 mt-4 text-center">
      <h2 class="user-heading"><%= @user.name %>さんのページ</h2>
      <p>登録情報の確認・更新ができます。</p>
    </div>
    <div class="col-md-6">
      <p class="user-item">名前（ニックネームでも可）</p>
      <%= @user.name %>
      <p class="user-item">メールアドレス</p>  
      <%= @user.email %><br>
      <%= link_to "ユーザー情報編集", edit_user_path, class:"btn btn-success mt-4" %><br>
      <%= link_to "ログインパスワード変更", user_newpassword_path(@user), class:"btn btn-info mt-3" %><br>
      <%= link_to "運営へ問い合わせ", new_contact_path, class:"btn btn-secondary mt-3" %>
      <%= link_to "退会", user_unsubscribe_path(@user), class:"btn btn-danger mt-3" %>
    </div>
    <div class="col-md-6">
      <h4 class="user-favorite-heading mt-3 mb-3">お気に入りサウナ</h4>
      <% if @favorite_list.present? %>
        <% @favorite_list.each do |favorite| %>
          <p class="user-favorite-item">・<%= favorite.sauna_name %>　室温：<%= favorite.temperature %>℃　水風呂：<%= favorite.temperature %>℃<br>
            <span class="ml-3" ><%= favorite.prefecture %></span>
            <%= link_to "詳細", facility_path(favorite.id), class:"btn ml-4" %><br>
            <%= render partial: 'public/favorites/userFavoriteDelete', locals: { favorite: favorite} %>
          </p>
        <% end %>
      <% else %>
        <p>お気に入りに登録されているサウナがありません。</p>
      <% end %>
    </div>
  </div>
</div>
